<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd"
         version="4.0">

    <!-- Security Constraint for API endpoints -->
    <security-constraint>
        <web-resource-collection>
            <web-resource-name>DragonApplication</web-resource-name>
            <url-pattern>/api/*</url-pattern> <!-- Secure API endpoints -->
        </web-resource-collection>
        <auth-constraint>
            <role-name>USER</role-name>
        </auth-constraint>
    </security-constraint>

    <!-- Publicly accessible static resources (Vue app) -->
    <security-constraint>
        <web-resource-collection>
            <web-resource-name>StaticContentResource</web-resource-name>
            <url-pattern>/frontend/*</url-pattern> <!-- Allow Vue files -->
        </web-resource-collection>
        <auth-constraint>

        </auth-constraint>
        <user-data-constraint>
            <transport-guarantee>NONE</transport-guarantee>
        </user-data-constraint>
    </security-constraint>

    <!-- Define roles -->
    <security-role>
        <role-name>USER</role-name>
    </security-role>

    <!-- Login Configuration -->
    <login-config>
        <auth-method>FORM</auth-method>
        <form-login-config>
            <!-- Route to Vue app -->
            <form-login-page>index.html</form-login-page>
            <form-error-page>index.html</form-error-page>
        </form-login-config>
    </login-config>

    <!-- Welcome file for the Vue app -->
    <welcome-file-list>
        <welcome-file>index.html</welcome-file>
    </welcome-file-list>
</web-app>
